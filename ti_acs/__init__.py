"""TI-acs reproduction package: minimal IO, style constants, and plot helpers.

Research methodology (Gini, demographic-group assignment, OLS regression,
segment decomposition) is intentionally kept inline in the notebooks so
readers can see and audit the paper's methods directly. This package only
holds repetitive file IO and matplotlib styling.
"""

__version__ = "1.0.0"
